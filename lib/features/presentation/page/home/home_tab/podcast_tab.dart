import 'package:flutter/material.dart';
class PodcastTab extends StatelessWidget {
  const PodcastTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is podcast tab"),
      ),
    );
  }
}
